main() {
  List<String> names = ['Son', ' PhuongAnh', 'hello'];
  // names.add('Max');
  // names.addAll(['max', 'nick', 'linda']);
  // names.insert(1, 'yeu');
  // names.insertAll(1, ['yue', 'yeeu']);
  // names.remove('hello');
  // names.removeAt(1);
  names.removeLast();
  print(names);
}
